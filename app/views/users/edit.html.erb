<% provide :title, t("edit.title") %>
<% provide :button_text, t("edit.save") %>
<h1><%= t "edit.update" %></h1>
<div class="row">
  <div class="mx-auto col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("edit.change"), Settings.link.gravatar_email, target: "_blank" %>
    </div>
  </div>
</div>
